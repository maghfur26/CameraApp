import { View } from 'react-native';
import MaterialIcons from '@react-native-vector-icons/material-icons';

const Profile = () => {
  return (
    <View>
      <MaterialIcons name="account-circle" size={30} color="#4B4DED" />
    </View>
  );
};

export default Profile;
